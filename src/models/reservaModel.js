// MODEL — Reservas
// TODO: importar db -> const db = require('../db');

// criarReserva({ tutor_id, pet_id, servico_id, data_inicio, data_fim, valor_total })
// TODO: INSERT INTO reserva (...) VALUES (...) RETURNING *

// listarReservasPorTutor(tutorId)
// TODO: SELECT com JOINs em pet, servico, prestador WHERE tutor_id = $1

module.exports = {
  // criarReserva,
  // listarReservasPorTutor,
};
